const user = {
    id: 1,
    name: "TOTO",
    email: "toto@yopmail.com",
    address: {
        city: "Biarritz",
        zipcode: "64000",
        country: "FRANCE"
    }
}

function getUserEmail(param) {
    return // ???
}

function getUserEmail_destructured({email}) {
    return // ???;
}

// Boucler sur un objet avec Object.keys

function loopOnObjectKeys() {}

// Boucler sur un objet avec Object.values

function loopOnObjectValues() {}