const WCSArray = require("./base")

/*
  TODO: Ajouter la fonction removeItem_v2 à l'objet WCSArray qui
  prend 2 paramètres 1 table de string et une string
  supprime la string (2nd paramètre) du tableau si elle existe (comme sur le removeItem_v1 déjà présent)

  Retourne un tableau qui concatène chaque string du tableau avec son index:

  removeItem_v2( ["a", "b", "c", "d"], "d") devrait donc retourner: [ "a-0", "b-1", "c-2" ]
* */

function addRemoveV2Func(WCSArrayObj) {
  // ... Your code should be here only
}


module.exports = addRemoveV2Func(WCSArray);
